name: Release Android (Play Store)

# Auto-releases the mobile app to Google Play's production track at a 20%
# staged rollout. Deliberately scoped to the `production` branch only --
# pushes to `main` never trigger a store release; promotion happens via the
# PR opened by promote-to-production.yml.
#
# Staged (not 100% instant) on purpose: Google Play's automatic bad-release
# detection can only halt a rollout that hasn't already reached everyone.
# Advancing a healthy release to 100% is a manual step in Play Console.
on:
  push:
    branches: [production]
    paths:
      - 'apps/mobile-app/**'
      - '.github/workflows/release-android.yml'

jobs:
  release:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/mobile-app

    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.44.1'
          channel: 'stable'
          cache: true

      - name: Cache pub packages
        uses: actions/cache@v4
        with:
          path: ~/.pub-cache
          key: ${{ runner.os }}-pub-${{ hashFiles('apps/mobile-app/pubspec.lock') }}
          restore-keys: ${{ runner.os }}-pub-

      - name: Cache Gradle
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('apps/mobile-app/android/**/*.gradle*', 'apps/mobile-app/android/gradle/wrapper/gradle-wrapper.properties') }}
          restore-keys: ${{ runner.os }}-gradle-

      - name: Install dependencies
        run: flutter pub get

      - name: Decode signing keystore
        run: |
          mkdir -p android/app/keystore
          echo "${{ secrets.KEYSTORE_FILE_BASE64 }}" | base64 -d > android/app/keystore/mali-up-release.jks

      - name: Build release App Bundle
        env:
          KEYSTORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
        # Version code = minutes since Unix epoch. Play Console requires every
        # upload's version code to exceed every version code it has ever seen
        # for this app; github.run_number (starts at 1 per-workflow) collided
        # with an already-published release. Epoch-minutes is monotonic
        # forever and needs no manual bookkeeping.
        run: flutter build appbundle --release --build-number=$(( $(date +%s) / 60 ))

      - name: Publish to Play Store (staged rollout)
        uses: r0adkll/upload-google-play@v1
        with:
          serviceAccountJsonPlainText: ${{ secrets.PLAY_SERVICE_ACCOUNT_JSON }}
          packageName: com.neuraltale.maliup
          releaseFiles: apps/mobile-app/build/app/outputs/bundle/release/app-release.aab
          track: production
          userFraction: 0.2
          status: inProgress

      - name: Remove decoded keystore
        if: always()
        run: rm -f android/app/keystore/mali-up-release.jks
