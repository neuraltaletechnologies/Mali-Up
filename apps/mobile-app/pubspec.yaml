name: mali_up
description: "Mali Up - Business Management App for Tanzanian SMEs"
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used by the build suffix.
#
# release-android.yml auto-bumps the patch number here after each successful
# Play Store upload and commits it back to main, so this always reflects the
# version currently live (or about to ship). Bump minor/major by hand in the
# PR when a release deserves it; CI keeps patch-bumping from whatever you set.
version: 1.2.2+3

environment:
  sdk: ^3.10.4

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  live_activities: ^2.4.9
  flutter_riverpod: ^2.6.1
  flutter_slidable: ^3.1.1
  go_router: ^17.1.0
  fl_chart: ^1.2.0
  google_fonts: ^8.0.2
  firebase_core: 4.13.0 # pinned: firebase_auth 6.5.7's Android glue still references a static
    # FlutterFirebaseCorePlugin.customAuthDomain field that firebase_core 4.14.0 removed
    # (moved to an instance member on FlutterFirebasePlugin), breaking the Android build.
    # Unpin once firebase_auth ships a release compatible with firebase_core >=4.14.0.
  lottie: ^3.3.2
  smooth_page_indicator: ^1.1.0
  firebase_auth: ^6.5.4
  cloud_firestore: ^6.6.0
  shared_preferences: ^2.3.2
  flutter_secure_storage: ^9.2.2
  cloud_functions: ^6.3.3
  http: ^1.2.1
  url_launcher: ^6.3.1
  package_info_plus: ^8.1.2
  in_app_review: ^2.0.9
  connectivity_plus: ^6.0.1
  image_picker: ^1.1.2
  firebase_storage: ^13.4.3
  flutter_contacts: ^1.1.9+2
  permission_handler: ^12.0.0
  local_auth: ^2.3.0
  mobile_scanner: ^7.0.0
  barcode_widget: ^2.0.3
  freezed_annotation: ^3.0.0
  pdf: ^3.11.0
  printing: ^5.13.2
  open_filex: ^4.5.0
  path_provider: ^2.1.5
  intl: ^0.20.2
  drift: ^2.33.0
  drift_flutter: ^0.3.0
  sqlite3_flutter_libs: ^0.6.0+eol
  uuid: ^4.5.3
  crypto: ^3.0.7
  font_awesome_flutter: ^11.0.0
  flutter_local_notifications: ^21.0.0
  firebase_messaging: ^16.5.0
  firebase_app_check: ^0.4.6 # pinned alongside firebase_core (see note above); 0.4.7 forces firebase_core ^4.14.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: ^0.14.4
  flutter_lints: ^6.0.0
  freezed: ^3.0.0
  build_runner: ^2.15.0
  drift_dev: ^2.33.0


# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/branding/
    - assets/lottie/
    - assets/Picture/
    # Bundled Google Fonts (DM Sans, JetBrains Mono, DM Serif Display, DM Mono).
    # google_fonts picks these up by filename so the app never fetches fonts
    # over the network — see GoogleFonts.config.allowRuntimeFetching in main.dart.
    - assets/google_fonts/

flutter_launcher_icons:
  android: true
  ios: true
  image_path: assets/branding/mali_up_wordmark.png
  adaptive_icon_background: "#FACC15"
  adaptive_icon_foreground: assets/branding/mali_up_wordmark.png
  remove_alpha_ios: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
