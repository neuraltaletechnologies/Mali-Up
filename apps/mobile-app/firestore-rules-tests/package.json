{
  "name": "mali-up-firestore-rules-tests",
  "private": true,
  "type": "module",
  "description": "Firestore security rules tests, run against the local emulator. Not part of the Flutter app — see README.md for how to run.",
  "scripts": {
    "test": "node --test rules.test.mjs"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.14.1"
  }
}
