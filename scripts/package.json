{
  "name": "maliup-scripts",
  "version": "1.0.0",
  "description": "One-time admin scripts for Mali Up",
  "main": "seed-master-catalog.js",
  "scripts": {
    "seed": "node seed-master-catalog.js",
    "seed:catalog": "ts-node seed-master-catalog.ts",
    "seed:catalog:force": "ts-node seed-master-catalog.ts --force",
    "seed:dry-run": "ts-node seed-master-catalog.ts --dry-run",
    "backfill:barcodes": "ts-node backfill-barcodes.ts",
    "backfill:barcodes:dry": "ts-node backfill-barcodes.ts --dry-run",
    "backfill:barcodes:count": "ts-node backfill-barcodes.ts --count-only"
  },
  "dependencies": {
    "firebase-admin": "^13.10.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.0.0"
  }
}
